import { SubtractDatePipe } from './subtract-date.pipe';

describe('SubtractDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SubtractDatePipe();
    expect(pipe).toBeTruthy();
  });
});
